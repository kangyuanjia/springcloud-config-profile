server:
  port: 9000
spring:
  profiles:
    active: dev
#zuul:
#  routes:
#    kyj-cloud-consumer: /consumer/**
#    kyj-cloud-provider: /provider/**
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 80000
